<UserControl
    x:Class="MoreConvenientJiraSvn.App.Views.Controls.ListBoxInput"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MoreConvenientJiraSvn.App.Views.Controls"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="200"
    d:DesignWidth="160"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <materialDesign:AutoSuggestBox
            x:Name="InputTextBox"
            Grid.Row="0"
            Grid.Column="0"
            VerticalAlignment="Bottom"
            DisplayMember="Value"
            Suggestions="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <Button
            Grid.Row="0"
            Grid.Column="1"
            Click="AddButton_Click"
            Content="+" />
        <ListBox x:Name="ItemsList" ItemsSource="{Binding SelectedItems, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <TextBlock VerticalAlignment="Center" Text="{Binding}" />
                        <Button
                            Margin="5,0"
                            Click="DeleteButton_Click"
                            Content="-"
                            DockPanel.Dock="Right" />
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
