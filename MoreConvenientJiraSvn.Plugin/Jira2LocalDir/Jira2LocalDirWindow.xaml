<Window
    x:Class="MoreConvenientJiraSvn.Plugin.Jira2LocalDir.Jira2LocalDirWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:control="clr-namespace:MoreConvenientJiraSvn.Plugin"
    xmlns:convert="clr-namespace:MoreConvenientJiraSvn.Plugin.Convert"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MoreConvenientJiraSvn.Plugin.Jira2LocalDir"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:MoreConvenientJiraSvn.Core.Models;assembly=MoreConvenientJiraSvn.Core"
    Title="Jira本地化"
    Width="1280"
    Height="720"
    d:DataContext="{d:DesignInstance Type=local:Jira2LocalDirViewModel}"
    Style="{StaticResource MaterialDesignWindow}"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme
                    BaseTheme="Light"
                    PrimaryColor="DeepPurple"
                    SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <local:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter" />
            <Style BasedOn="{StaticResource MaterialDesignBody1TextBlock}" TargetType="TextBlock">
                <Setter Property="Margin" Value="2" />
            </Style>
            <Style x:Key="StackPanelStyle" TargetType="Border">
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="BorderBrush" Value="Gray" />
                <Setter Property="Margin" Value="6" />
                <Setter Property="Padding" Value="6" />
            </Style>
            <DataTemplate x:Key="ComboBoxTemplate" DataType="{x:Type model:JiraField}">
                <StackPanel Orientation="Vertical" DataContext="{Binding ElementName=ContentControl, Path=DataContext}">
                    <TextBlock MinWidth="120" Text="{Binding FieldName}"/>
                    <ComboBox MinWidth="120" ItemsSource="{Binding Options}" SelectedItem="{Binding FieldValue}" DisplayMemberPath="{Binding OptionName}" />
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:Key="TextBoxTemplate" DataType="{x:Type model:JiraField}">
                <StackPanel Orientation="Vertical" DataContext="{Binding ElementName=ContentControl, Path=DataContext}">
                    <TextBlock MinWidth="120" Text="{Binding FieldName}"/>
                    <TextBox MinWidth="120" Text="{Binding FieldValue}" />
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:Key="BigTextBoxTemplate" DataType="{x:Type model:JiraField}">
                <StackPanel Orientation="Vertical" DataContext="{Binding ElementName=ContentControl, Path=DataContext}">
                    <TextBlock MinWidth="120" Text="{Binding FieldName}"/>
                    <TextBox AcceptsReturn="True"
                               TextWrapping="Wrap"
                             Padding="4"
                             MinHeight="60"
                            VerticalScrollBarVisibility="Auto"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        MinWidth="120" Text="{Binding FieldValue}" />
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:Key="ListBoxTemplate" DataType="{x:Type model:JiraField}">
                <StackPanel Orientation="Vertical" DataContext="{Binding ElementName=ContentControl, Path=DataContext}">
                    <TextBlock MinWidth="120" Text="{Binding FieldName}"/>
                    <ListBox MinWidth="120" ItemsSource="{Binding Options}" SelectionChanged="ListBox_SelectionChanged"  
    SelectionMode="Multiple">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type model:FieldOption}">
                                <TextBlock Text="{Binding OptionName}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox> 
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:Key="DatePickerTemplate" DataType="{x:Type model:JiraField}">
                <StackPanel Orientation="Vertical" DataContext="{Binding ElementName=ContentControl, Path=DataContext}">
                    <TextBlock MinWidth="120" Text="{Binding FieldName}"/>
                    <DatePicker MinWidth="120" SelectedDateFormat="Short" SelectedDate="{Binding FieldValue}" />
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <materialDesign:DrawerHost
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        BorderBrush="{DynamicResource MaterialDesignDivider}"
        BorderThickness="2"
        
        BottomDrawerBackground="{DynamicResource SecondaryHueLightBrush}"
        BottomDrawerCornerRadius="20 20 0 0">
        <materialDesign:DrawerHost.Style>
            <Style BasedOn="{StaticResource {x:Type materialDesign:DrawerHost}}" TargetType="materialDesign:DrawerHost">
                <!--<Style.Triggers>
                    <DataTrigger Binding="{Binding IsChecked, ElementName=BackgroundToggle}" Value="True">
                        <Setter Property="OverlayBackground" Value="{DynamicResource PrimaryHueMidBrush}" />
                    </DataTrigger>
                </Style.Triggers>-->
            </Style>
        </materialDesign:DrawerHost.Style>
        <materialDesign:DrawerHost.LeftDrawerContent>
            <StackPanel Margin="16,16,16,16" Width="400">
                <ListBox
                    ItemsSource="{Binding Transitions}"
                    SelectedValue="{Binding SelectedTransition}"
                    Style="{StaticResource MaterialDesignChoiceChipSecondaryListBox}" />
                <ItemsControl ItemsSource="{Binding FieldModels}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type model:JiraField}" >
                            <ContentControl x:Name="ContentControl" Margin="4,8,4,8">
                                <ContentControl.Style>
                                    <Style TargetType="ContentControl">
                                        <Setter Property="DataContext" Value="{Binding}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="ComboBox">
                                                <Setter Property="ContentTemplate" Value="{StaticResource ComboBoxTemplate}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}" Value="TextBox">
                                                <Setter Property="ContentTemplate" Value="{StaticResource TextBoxTemplate}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}" Value="BigTextBox">
                                                <Setter Property="ContentTemplate" Value="{StaticResource BigTextBoxTemplate}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}" Value="ListBox">
                                                <Setter Property="ContentTemplate" Value="{StaticResource ListBoxTemplate}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}" Value="DatePicker">
                                                <Setter Property="ContentTemplate" Value="{StaticResource DatePickerTemplate}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button
                    Margin="4"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
                    CommandParameter="{x:Static Dock.Left}"
                    Content="取消"
                    Style="{StaticResource MaterialDesignFlatButton}" />
            </StackPanel>
        </materialDesign:DrawerHost.LeftDrawerContent>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="720" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  Left jira title  -->
            <DockPanel
                Grid.Row="0"
                Grid.Column="0"
                Margin="16">

                <!--  Jira search bar  -->
                <DockPanel
                    Grid.Row="0"
                    HorizontalAlignment="Stretch"
                    DockPanel.Dock="Top">
                    <ComboBox
                        x:Name="SelectTypeComboBox"
                        MinWidth="64"
                        DockPanel.Dock="Left"
                        ItemsSource="{Binding JiraQueryTypes}"
                        SelectedItem="{Binding SelectedJiraQueryType}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={x:Static convert:JiraSelectTypeToStringConverter.Instance}}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Grid MinWidth="256" HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <ComboBox
                            Width="240"
                            ItemsSource="{Binding JiraFilters}"
                            SelectedItem="{Binding SelectedFilter}"
                            Visibility="{Binding IsUseTextToSearch, Converter={x:Static convert:InverseBooleanToVisibilityConverter.Instance}}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <TextBox
                            Width="240"
                            Text="{Binding JiraQueryText}"
                            Visibility="{Binding IsUseTextToSearch, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </Grid>
                    <Button
                        x:Name="JiraSearchButton"
                        MinWidth="128"
                        HorizontalAlignment="Right"
                        Command="{Binding QueryJiraInfoCommand}"
                        Content="搜索"
                        DockPanel.Dock="Right"
                        Style="{StaticResource MaterialDesignRaisedSecondaryDarkButton}" />
                </DockPanel>

                <!--  Jira list  -->
                <DataGrid
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    Foreground="Black"
                    GridLinesVisibility="Horizontal"
                    HorizontalGridLinesBrush="Gray"
                    HorizontalScrollBarVisibility="Auto"
                    IsReadOnly="True"
                    ItemsSource="{Binding JiraInfoList}"
                    ScrollViewer.HorizontalScrollBarVisibility="Visible"
                    ScrollViewer.VerticalScrollBarVisibility="Visible"
                    SelectedItem="{Binding SelectedJiraInfo}"
                    SelectionChanged="DataGrid_SelectionChanged"
                    SelectionMode="Extended"
                    SelectionUnit="FullRow"
                    VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding JiraId}"
                            Header="编号" />
                        <DataGridTextColumn
                            Width="3*"
                            Binding="{Binding Summary}"
                            Header="主题" />
                        <DataGridTextColumn
                            Width="1*"
                            Binding="{Binding IssueTypeName}"
                            Header="类型" />
                        <DataGridTextColumn
                            Width="1.5*"
                            Binding="{Binding FixVersionNameText}"
                            Header="修复版本" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>

            <!--  Right jira detail  -->
            <Grid
                Grid.Row="0"
                Grid.Column="1"
                Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ScrollViewer
                    Grid.Row="0"
                    HorizontalScrollBarVisibility="Disabled"
                    VerticalScrollBarVisibility="Auto">
                    <DockPanel>
                        <!--  Common setting  -->
                        <Expander
                            Margin="6"
                            DockPanel.Dock="Top"
                            Header="公共设置">
                            <StackPanel>
                                <control:ConfigLine Margin="8" ConfigName="默认地址">
                                    <DockPanel>
                                        <TextBox
                                            MinWidth="160"
                                            HorizontalAlignment="Left"
                                            DockPanel.Dock="Left"
                                            IsReadOnly="True"
                                            Style="{StaticResource MaterialDesignTextBox}"
                                            Text="{Binding LocalJiraSetting.ParentDir}" />
                                        <Button
                                            Width="60"
                                            HorizontalAlignment="Right"
                                            Command="{Binding SetLocalDirForJiraInfoCommand}"
                                            Content="选择"
                                            DockPanel.Dock="Right" />
                                    </DockPanel>
                                </control:ConfigLine>
                                <control:ConfigLine Margin="8" ConfigName="注释时的用户名">
                                    <DockPanel>
                                        <TextBox
                                            MinWidth="160"
                                            HorizontalAlignment="Left"
                                            DockPanel.Dock="Left"
                                            IsReadOnly="False"
                                            Text="{Binding LocalJiraSetting.UserName}" />
                                        <Button
                                            Width="60"
                                            HorizontalAlignment="Right"
                                            Command="{Binding SetUserNameForJiraInfoCommand}"
                                            Content="设置"
                                            DockPanel.Dock="Right" />
                                    </DockPanel>
                                </control:ConfigLine>
                            </StackPanel>

                        </Expander>

                        <!--  Local Operation  -->
                        <Expander
                            Margin="6"
                            DockPanel.Dock="Top"
                            Header="本地存档"
                            IsExpanded="True">
                            <WrapPanel>
                                <TextBlock Text="{Binding SelectedJiraLocalInfo.LocalDir}" />
                                <Button Command="{Binding UseSelectedToCreateLocalJiraCommand}" Content="{Binding LocalJiraOperationText}" />
                                <Button Command="{Binding CopyCommitTextCommand}" Content="复制提交信息" />
                                <Button Command="{Binding CopyAnnotationTextCommand}" Content="复制注释信息" />
                                <Button Command="{Binding OpenWebPageCommand}" Content="打开Jira网页" />
                                <Button
                                    Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
                                    CommandParameter="{x:Static Dock.Left}"
                                    Content="修改Jira状态" />
                            </WrapPanel>
                        </Expander>

                        <!--  Jira info  -->
                        <Expander
                            Margin="6"
                            DockPanel.Dock="Top"
                            Header="Jira信息"
                            IsExpanded="True">
                            <WrapPanel>
                                <Border Style="{StaticResource StackPanelStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="Jira ID:" />
                                        <TextBox
                                            IsReadOnly="True"
                                            Style="{StaticResource MaterialDesignFilledTextBox}"
                                            Text="{Binding SelectedJiraInfo.JiraId, Mode=OneWay}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource StackPanelStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="Summary:" />
                                        <TextBox
                                            IsReadOnly="True"
                                            Style="{StaticResource MaterialDesignFilledTextBox}"
                                            Text="{Binding SelectedJiraInfo.Summary, Mode=OneWay}"
                                            TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource StackPanelStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="Status:" />
                                        <TextBox
                                            IsReadOnly="True"
                                            Style="{StaticResource MaterialDesignFilledTextBox}"
                                            Text="{Binding SelectedJiraInfo.StatusName, Mode=OneWay}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource StackPanelStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="Priority:" />
                                        <TextBox
                                            IsReadOnly="True"
                                            Style="{StaticResource MaterialDesignFilledTextBox}"
                                            Text="{Binding SelectedJiraInfo.PriorityName, Mode=OneWay}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource StackPanelStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="Creator:" />
                                        <TextBox
                                            IsReadOnly="True"
                                            Style="{StaticResource MaterialDesignFilledTextBox}"
                                            Text="{Binding SelectedJiraInfo.CreatorName, Mode=OneWay}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource StackPanelStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="Issue Type:" />
                                        <TextBox
                                            IsReadOnly="True"
                                            Style="{StaticResource MaterialDesignFilledTextBox}"
                                            Text="{Binding SelectedJiraInfo.IssueTypeName, Mode=OneWay}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource StackPanelStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="Fix Versions:" />
                                        <TextBox
                                            IsReadOnly="True"
                                            Style="{StaticResource MaterialDesignFilledTextBox}"
                                            Text="{Binding SelectedJiraInfo.FixVersionNameText, Mode=OneWay}"
                                            TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource StackPanelStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="Resolution:" />
                                        <TextBox
                                            IsReadOnly="True"
                                            Style="{StaticResource MaterialDesignFilledTextBox}"
                                            Text="{Binding SelectedJiraInfo.ResolutionName, Mode=OneWay}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource StackPanelStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="Developer Leader:" />
                                        <TextBox
                                            IsReadOnly="True"
                                            Style="{StaticResource MaterialDesignFilledTextBox}"
                                            Text="{Binding SelectedJiraInfo.DeveloperLeaderName, Mode=OneWay}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource StackPanelStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="Developers:" />
                                        <TextBox
                                            IsReadOnly="True"
                                            Style="{StaticResource MaterialDesignFilledTextBox}"
                                            Text="{Binding SelectedJiraInfo.DeveloperNamesString, Mode=OneWay}"
                                            TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>
                            </WrapPanel>
                        </Expander>

                        <!--  Link Jira  -->
                        <Expander
                            Margin="6"
                            DockPanel.Dock="Top"
                            Header="关联Jira">
                            <DockPanel Margin="8">
                                <ListBox />
                            </DockPanel>
                        </Expander>

                        <!--  Svn logs  -->
                        <Expander
                            Margin="6"
                            DockPanel.Dock="Top"
                            Header="Svn信息">
                            <DockPanel Margin="8">
                                <Grid DockPanel.Dock="Top">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>

                                    <TextBlock
                                        Grid.Row="0"
                                        Grid.Column="0"
                                        Margin="4"
                                        Text="所选Svn路径" />
                                    <ComboBox
                                        Grid.Row="0"
                                        Grid.Column="1"
                                        Grid.ColumnSpan="3"
                                        Margin="4"
                                        DisplayMemberPath="FullPathName"
                                        IsEnabled="{Binding IsNotQuerySvnLoging}"
                                        ItemsSource="{Binding RelatSvnPaths}"
                                        SelectedItem="{Binding SelectedSvnPath}"
                                        SelectionChanged="ComboBox_SelectionChanged" />

                                    <TextBlock
                                        Grid.Row="1"
                                        Grid.Column="0"
                                        Grid.ColumnSpan="2"
                                        Text="{Binding SvnLogVersionRangeText}" />
                                    <TextBlock
                                        Grid.Row="1"
                                        Grid.Column="2"
                                        Grid.ColumnSpan="2"
                                        Text="{Binding SvnLogTimeRangeText}" />

                                    <Button
                                        Grid.Row="2"
                                        Grid.Column="0"
                                        Margin="8"
                                        Command="{Binding DownloadPrevSvnLogByAmountCommand}"
                                        CommandParameter="100"
                                        Content="Prev 100" />
                                    <Button
                                        Grid.Row="2"
                                        Grid.Column="1"
                                        Margin="8"
                                        Command="{Binding DownloadNextSvnLogByAmountCommand}"
                                        CommandParameter="100"
                                        Content="Next 100" />
                                    <Button
                                        Grid.Row="2"
                                        Grid.Column="2"
                                        Margin="8"
                                        Command="{Binding DownloadPrevSvnLogByDateCommand}"
                                        CommandParameter="1"
                                        Content="Prev 1 Day" />
                                    <Button
                                        Grid.Row="2"
                                        Grid.Column="3"
                                        Margin="8"
                                        Command="{Binding DownloadNextSvnLogByDateCommand}"
                                        CommandParameter="1"
                                        Content="Next 1 Day" />
                                </Grid>

                                <DataGrid
                                    AutoGenerateColumns="False"
                                    CanUserAddRows="False"
                                    DockPanel.Dock="Bottom"
                                    Foreground="Black"
                                    GridLinesVisibility="Horizontal"
                                    HorizontalGridLinesBrush="Gray"
                                    HorizontalScrollBarVisibility="Auto"
                                    IsReadOnly="True"
                                    ItemsSource="{Binding SelectedJiraSvnLogs}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                    ScrollViewer.VerticalScrollBarVisibility="Visible"
                                    SelectionMode="Extended"
                                    SelectionUnit="FullRow"
                                    VerticalScrollBarVisibility="Auto">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn
                                            Width="1*"
                                            Binding="{Binding Revision}"
                                            Header="版本" />
                                        <DataGridTextColumn
                                            Width="1*"
                                            Binding="{Binding Operation}"
                                            Header="操作类型" />
                                        <DataGridTextColumn
                                            Width="1*"
                                            Binding="{Binding Author}"
                                            Header="作者" />
                                        <DataGridTextColumn
                                            Width="1*"
                                            Binding="{Binding DateTime}"
                                            Header="更新日期" />
                                        <DataGridTextColumn
                                            Width="3*"
                                            Binding="{Binding Message}"
                                            Header="信息" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </DockPanel>
                        </Expander>

                    </DockPanel>
                </ScrollViewer>
            </Grid>

        </Grid>
    </materialDesign:DrawerHost>
</Window>
